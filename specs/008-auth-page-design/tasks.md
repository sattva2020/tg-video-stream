---
description: "Детализированный план задач для фичи Auth Page Design"
---

# Tasks: Auth Page Design

**Input**: `/specs/008-auth-page-design/` (spec.md, plan.md, research.md, data-model.md, contracts/, quickstart.md)
**Prerequisites**: Конституция 1.0.0, ветка `008-auth-page-design`

**Tests**: Требуются Playwright (desktop + 320px), Vitest снапшоты, Lighthouse > 90. Все результаты логируем в `.internal/frontend-logs/` (директория добавлена в `.gitignore`); `frontend/logs/` используется только для публичных артефактов по согласованию.

**Organization**: Задачи сгруппированы по фазам: Setup → Foundational → User Stories (US1–US3) → Polish. User stories независимы и тестируются отдельными сценариями.

## Format: `[ID] [P?] [Story] Description`

- **[P]** — возможно выполнять параллельно (разные файлы, нет зависимостей)
- **[US#]** — метка пользовательской истории
- Всегда указываем полноценный путь к файлу

> ⚖️ Конституция: соблюдаем хранение логов в `.internal/`/`frontend/logs/`, обновляем `docs/`, тесты размещаем в `frontend/tests/`, окружение меняем только через `template.env`.

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: подготовить зависимости и рабочие каталоги до реализации историй

- [x] T001 Обновить `frontend/package.json` и `package-lock.json`, добавив `aceternity-ui`, `magic-ui`, `hero-ui`, `@react-three/drei`, `lighthouse` (dev) и `i18next` (для локализации), затем выполнить `npm install` для фиксации версий и базовой настройки i18next.
- [x] T002 Добавить npm-скрипты `test:ui` (Playwright) и `lighthouse:auth` в `frontend/package.json`, чтобы автоматизировать проверки SC-002/SC-003.
- [x] T003 Создать приватную директорию `.internal/frontend-logs/.gitkeep` и добавить её в корневой `.gitignore` для хранения отчётов Playwright/Lighthouse. `frontend/logs/` оставляем только для публичных артефактов по согласованию.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: общие артефакты (токены, хуки, API клиенты, тестовые каркасы), без которых user stories невозможны

- [x] T004 Создать `frontend/src/styles/tokens.css` с CSS custom properties для светлой (`colorParchmentLight`, `colorInkLight`) и тёмной (`colorNightSky`, `colorInkDark`) тем из data-model.md.
- [x] T005 Обновить `frontend/tailwind.config.js` и `frontend/src/main.tsx`, чтобы подключить токены, шрифты LandingSans/Serif и кастомные spacing/transition scale.
- [x] T006 Создать хук `frontend/src/hooks/useThemePreference.ts` (Zustand + `prefers-color-scheme`) и экспортировать `ThemePreferenceProvider` для дальнейшего использования.
- [x] T007 Реализовать `frontend/src/lib/api/authClient.ts`, покрывающий `/api/auth/login`, `/api/auth/register`, `/api/users/me` по `specs/008-auth-page-design/contracts/auth-ui.yaml`.
- [x] T008 Создать `frontend/tests/playwright/auth-page.spec.ts` с тремя `describe` блоками (theme/responsive/errors) и временно падающими `test.skip()` — they'll be enabled per story.
- [x] T009 Добавить `frontend/tests/vitest/setup.ts` и обновить `vitest.config.ts`, чтобы Magic UI/Aceternity UI компоненты корректно рендерились в снапшотах.

**Checkpoint**: когда T001–T009 завершены, инфраструктура готова к независимой разработке user stories.

---

## Phase 3: User Story 1 – Consistent Visual Theme (Priority: P1) 🎯 MVP

**Goal**: страница авторизации и приветственный блок dashboard визуально совпадают с лендингом (фон, типографика, токены). Dual-theme функциональность реализуется в US4.

**Independent Test**: Запустить Playwright `auth-page.spec.ts --grep theme` и убедиться, что фон, шрифты и токены совпадают с Landing; экран не содержит «чужих» цветов и шрифтов.

### Tests (US1)

- [x] T010 [P] [US1] Реализовать в `frontend/tests/playwright/auth-page.spec.ts` проверки шрифтов, цветовой палитры и фоновых токенов (без переключения тем) с `test.fail()` до готовности.
- [x] T011 [P] [US1] Добавить Vitest снапшоты `frontend/tests/vitest/auth-card.spec.tsx`, фиксирующие визуальное соответствие светлой палитре LandingSans/Serif.

### Implementation (US1)

- [x] T012 [P] [US1] Настроить `frontend/src/pages/AuthPage3D.tsx`, чтобы фон, типографика и layout использовали токены ZenScene без переключателя тем.
- [x] T013 [P] [US1] Реализовать `frontend/src/components/auth/AuthCard.tsx` на Aceternity/Magic UI с использованием токенов и типографики.
- [x] T014 [US1] Интегрировать новые стили в `frontend/src/pages/AuthPage3D.tsx`, подключив `AuthCard`, фон ZenScene и статический dashboard preview.
- [x] T016 [US1] Обновить `docs/auth-page-ui.md`, описав токены и скриншоты визуального соответствия лендингу.

**Checkpoint**: после T010–T016 User Story 1 готова к демонстрации как MVP.

---

## Phase 4: User Story 2 – Responsive Design (Priority: P2)

**Goal**: форма/фон корректно отображаются от 320px до desktop, WebGL не блокирует ввод, есть статический fallback.

**Independent Test**: Playwright `auth-page.spec.ts --grep responsive` в режиме 320px, Lighthouse Mobile preset > 90, отсутствие горизонтального скролла.

### Tests (US2)

- [x] T017 [P] [US2] Добавить в `frontend/tests/playwright/auth-page.spec.ts` мобильный сценарий (viewport 320×640, touch), проверяющий отсутствие overflow и видимость CTA.
- [x] T017a [P] [US2] Добавить отдельный сценарий с viewport 280×640 (extra-small), проверить отсутствие горизонтального скролла и видимость основных CTA.

### Implementation (US2)

- [x] T018 [P] [US2] Создать `frontend/src/components/auth/AuthLayout.tsx` (flex/grid) с брейкпоинтами base/md/xl из data-model.md.
- [x] T019 [US2] Рефакторить `frontend/src/pages/AuthPage3D.tsx`, чтобы использовать `AuthLayout`, `React.Suspense` и ленивую загрузку 3D сцены (TTI < 2 c).
- [x] T020 [US2] Добавить `frontend/public/fallback/zen-scene.svg` и компонент `frontend/src/components/auth/ZenScene.tsx`, который переключается на статический фон при отсутствии WebGL или query `?forceStatic=1`.
- [x] T021 [US2] Задокументировать адаптивность и fallback в `docs/development/frontend-theme.md` (секция Responsive Auth Flow).

**Checkpoint**: после T017–T021 страница авторизации полностью адаптивна и покрывает edge cases EC-001/002/003.

---

## Phase 5: User Story 3 – Error Message Styling (Priority: P3)

**Goal**: ошибки 403/409 визуально совместимы с темой, локализованы и дружелюбны.

**Independent Test**: Playwright `auth-page.spec.ts --grep errors` вызывает 403/409, отображаются стилизованные Toast/Inline блоки; Vitest подтверждает форматирование сообщений.

### Tests (US3)

- [ ] T022 [P] [US3] Добавить Vitest тесты `frontend/tests/vitest/error-toast.spec.tsx`, проверяющие состояния `conflict`, `pending`, `server`.
- [ ] T023 [P] [US3] Расширить `frontend/tests/playwright/auth-page.spec.ts`, замокав `/api/auth/register` (409) и `/api/auth/login` (403), чтобы проверять цвета/иконки сообщений.
- [ ] T023a [US3] Добавить pytest-модуль `backend/tests/test_auth_errors.py`, который вызывает `/api/auth/login|register` и `/api/users/me`, проверяя наличие `code` и `hint` в ответах 403/409 и соответствие enum `AuthError`. Тест должен также проверять, что либо присутствует локализованное `message`, либо `message_key` для клиентской локализации.

### Implementation (US3)

- [ ] T024 [P] [US3] Создать `frontend/src/components/auth/ErrorToast.tsx` (Hero UI Toast + Magic UI Alert) с поддержкой светлой/тёмной темы.
- [ ] T025 [US3] Реализовать `frontend/src/services/authService.ts`, который преобразует ответы `authClient` в `AuthFormState.error` (code/message/hint/severity) и подключить к форме.
- [ ] T026 [US3] Обновить `backend/src/api/auth.py` и контракт `contracts/auth-ui.yaml`: 403/409 ответы ДОЛЖНЫ содержать обязательное поле `code` и `hint`; поле `message` может быть возвращено уже локализованным если сервер поддерживает `Accept-Language`. Если сервер не локализует сообщения, он обязан возвращать `message_key` (например, `auth.email_registered`) — фронтенд использует `message_key` + i18next для отображения локализованного текста. Синхронизировать и покрыть pytest (T023a).
- [ ] T027 [US3] Дополнить `docs/auth-page-ui.md` разделом «Статусы и ошибки» с примерами сообщений и рекомендациями локализации.

**Checkpoint**: после T022–T027 ошибки оформлены и соответствуют требованиям FR-001..FR-005 + SC-001..SC-004.

---

## Phase 6: User Story 4 – Dual Theme & Dashboard Greeting (Priority: P1)

**Goal**: предоставить переключатель светлая/тёмная тема, сохранять выбор пользователя и синхронизировать оформление приветственного блока dashboard с AuthPage.

**Independent Test**: Playwright `auth-page.spec.ts --grep theme` переключает тему, проверяя, что auth форма и HeroPanel отражают изменения <200 мс, а значение сохраняется между перезагрузками; Vitest снапшоты HeroPanel/ThemeToggle подтверждают визуальные состояния.

### Tests (US4)

- [x] T028 [P] [US4] Добавить в `frontend/tests/playwright/auth-page.spec.ts` сценарий theme toggle (desktop), измеряющий время переключения и проверяющий сохранение выбора через `localStorage`.
- [x] T029 [P] [US4] Создать Vitest тест `frontend/tests/vitest/theme-toggle.spec.tsx`, покрывающий светлый/тёмный стейт `ThemeToggle` и HeroPanel.

### Implementation (US4)

- [x] T030 [P] [US4] Расширить `frontend/src/hooks/useThemePreference.ts` (localStorage, `prefers-color-scheme`, `syncDashboardTheme`) и реализовать `frontend/src/components/auth/ThemeToggle.tsx` на Hero UI Switch.
- [x] T031 [P] [US4] Обновить `frontend/src/components/dashboard/HeroPanel.tsx`, чтобы он подписывался на `ThemePreferenceProvider` и применял токены (`colorNightSky`, `colorParchmentLight`).
- [x] T032 [US4] Завершить интеграцию в `frontend/src/pages/AuthPage3D.tsx`: разместить `ThemeToggle`, прокинуть тему в dashboard preview, добавить подсказку при конфликте сохранённой/системной темы (EC-004).
- [x] T033 [US4] Обновить `docs/auth-page-ui.md` (секция Dual Theme & Dashboard) и приложить скриншоты обеих тем.

**Checkpoint**: после T028–T033 страница авторизации и dashboard выглядят консистентно, тема сохраняется.

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: финальные проверки, документация, соответствие конституции

- [x] T034 [P] Запустить `npm run test:unit && npx playwright test && npm run lighthouse:auth`, сохранив отчёты в `.internal/frontend-logs/` и приложив ссылки в PR (публичные артефакты — в `frontend/logs/` только по согласованию).
- [x] T035 Верифицировать инструкции `specs/008-auth-page-design/quickstart.md`, перепройдя команды и обновив шаги при расхождениях.
- [x] T036 [P] Выполнить `npm run docs:validate && npm run docs:report`, выгрузить отчёты в `docs/REPORTS/` и указать ссылки в README ветки.
- [x] T037 Подготовить запись в `OUTSTANDING_TASKS_REPORT.md` (если найдены риски/долг во время тестов) либо зафиксировать её отсутствие.

---

## Dependencies & Execution Order

- **Phase 1 → Phase 2**: Без зависимостей, но T001–T003 должны завершиться до настройки токенов.
- **Phase 2 → US1/US2/US3/US4**: Все пользовательские истории используют токены, хук темы и `authClient`, поэтому T004–T009 — обязательный блокер.
- **User Story Order**: Сначала US1 (визуальная база). После завершения US1 можно параллельно начинать US2 и US4 (они используют готовый UI/токены). US3 подключается после готовности `authClient` и макетов UI.
- **Backend change (T026)** зависит от `contracts/auth-ui.yaml` и должен быть выполнен до финальных тестов US3.
- **Polish** стартует только после завершения всех историй.

### User Story Dependency Graph

```text
Phase1 ─▶ Phase2 ─▶ US1 ─┐
                         ├─▶ US2 ─┐
                         └─▶ US4 ─┤─▶ Polish
Phase2 ────────────────▶ US3 ─────┘
```

- **US1**: T012 (настройка AuthPage3D) и T013 (AuthCard) помечены [P] и могут выполняться разными разработчиками, пока T014 интегрирует их в общий экран.
- **US4**: T030 (hook + ThemeToggle) и T031 (HeroPanel) идут параллельно, T032 объединяет интерфейс, а T033 документирует две темы.
- **US2**: T018 (AuthLayout) и T020 (ZenScene fallback) независимы и могут идти параллельно; T019 лишь связывает результат.
- **US3**: T024 (ErrorToast) и T026 (backend ответы) можно делать одновременно, пока T025 подключает сервис к форме.
- **Testing**: Все [P] Playwright/Vitest задачи (T010, T011, T017, T022, T023) можно запускать в разных CI-джобах.

## Implementation Strategy

### MVP (deliver US1)

1. Завершить Setup и Foundational (T001–T009).
2. Реализовать US1 (T010–T016) и US4 (T028–T033) для полноценного dual-theme MVP.
3. Запустить Playwright/Vitest по теме, обновить документацию — MVP готов для ревью.

### Incremental Delivery

1. **Drop 1**: US1 + US4 (Consistent Theme + Dashboard) — демонстрируем dual-theme вход и приветствие.
2. **Drop 2**: US2 (Responsive + fallback, включая 280px) — показываем стабильность mobile/WebGL edge cases.
3. **Drop 3**: US3 (Styled Errors) — завершаем пользовательские ошибки и backend copy.

### Team Parallelism

- После T009 можно разделить команду: один разработчик ведёт US1, второй US2, третий US4, четвёртый — US3.
- Тестировщик может готовить Playwright сценарии заранее (T010, T017, T017a, T022, T028).
- Документация (T016, T021, T027, T033, T035, T036) выполняется в параллельных ветках после готовности соответствующих историй.
