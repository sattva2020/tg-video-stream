# Tasks: Современный лендинг Telegram 24/7 Video Streamer

**Input**: Design documents from `/specs/001-modern-home-design/`
**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/

**Tests**: Требуются e2e-сценарии Playwright для каждой пользовательской истории (CTA, список преимуществ, локализация, визуальный фон), отдельные проверки доступности/адаптивности (<320 px, fallback языка, fallback шрифтов), Lighthouse-профиль по perf-бюджету и сценарий `landing-visual-metrics.spec.ts`, который фиксирует fps/latency и прикладывается в quickstart.md.

## Success Criteria Traceability

| Success Criterion | Целевые задачи |
|-------------------|----------------|
| SC-001 (CTA <3 c, 90% пользователей) | T011, T027 |
| SC-002 (95% корректных локализаций) | T012, T013, T014, **T038**, **T039**, T024 |
| SC-003 (Hero/CTA ≤2 c на 3G) | T020, T021, T026 |
| SC-004 (Понимание ценности) | T009, T031, T032, T027 |

**Organization**: Tasks are grouped by phase → user story (US1–US3). Каждый таск указывает точный путь.

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Можно выполнять параллельно (разные файлы, без зависимостей)
- **[Story]**: Маркер `[US1]`, `[US2]`, `[US3]` для пользовательских историй
- Все пути указаны относительно корня репозитория

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Подготовить структуру и базовые ресурсы для всех историй

- [x] T001 Создать каркас компонентов лендинга в `frontend/src/components/landing/` (создать `index.ts`, `LandingLayout.tsx`, `types.ts`) для дальнейшего переиспользования
- [x] T002 Обновить `frontend/tailwind.config.js`, добавив цветовую палитру/градиенты и blur-токены, которые будут использовать все секции лендинга
- [x] T003 Добавить директорию `frontend/public/assets/landing/` с `landing-poster.webp` и README об использовании fallback-ассетов

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Обеспечить маршрутизацию, контент и утилиты, без которых невозможна реализация историй

- [x] T004 Прописать маршрут `/` в `frontend/src/App.tsx`, подключив временный `LandingPage` и сохранив текущие auth-роуты
- [x] T005 Создать `frontend/src/lib/landing/content.ts` с объектом `HeroContent` и валидацией единственного CTA (использует типы из data-model.md)
- [x] T006 Реализовать `frontend/src/lib/landing/visualSupport.ts` (проверка WebGL, `prefers-reduced-motion`, выбор fallback слоя) для дальнейшего использования в US3
- [x] T038 Добавить `frontend/src/lib/landing/useLandingLocale.ts` (или эквивалентный модуль), который разбирает `Accept-Language`, применяет fallback `en`, сохраняет выбор в `localStorage` и экспортирует флаг подсказки для language switcher

**Checkpoint**: Базовая инфраструктура готова; можно параллельно приступать к пользовательским историям

---

## Phase 3: User Story 1 – Моментальный вход (Priority: P1) 🎯 MVP

**Goal**: Пользователь видит hero-блок «Telegram 24/7 Video Streamer» и единственную кнопку «Вход», ведущую на `/login`
**Independent Test**: Открыть `/`, убедиться, что CTA кликабелен и переводит на `/login` без дополнительных шагов

### Implementation for User Story 1

- [x] T007 [P] [US1] Добавить английские ключи `hero_*` и `cta_enter` в `frontend/src/i18n.ts` (label, title, subtitle, benefits, CTA)
- [x] T008 [P] [US1] Реализовать компонент CTA в `frontend/src/components/landing/PrimaryCTA.tsx`, который использует React Router для перехода на `/login`
- [x] T009 [P] [US1] Сверстать `HeroSection` в `frontend/src/components/landing/HeroSection.tsx`, отображающий бренд-лейбл, заголовок, подзаголовок и список преимуществ
- [x] T010 [US1] Собрать `frontend/src/pages/LandingPage.tsx`, подключив `LandingLayout`, `HeroSection`, CTA и передав контент из `lib/landing/content`
- [x] T011 [US1] Создать Playwright тест `frontend/tests/e2e/landing/landing-cta.spec.ts`, который проверяет рендер hero и переход CTA на `/login`
- [x] T031 [P] [US1/US2] Заполнить `HeroContent` и `frontend/src/i18n.ts` локализованным массивом из 3–4 преимуществ, отрисовать его в `HeroSection`, добавить fallback на английский для отсутствующих ключей и обеспечить ARIA-описания для считывателей экрана
- [x] T032 [US1/US2] Создать e2e-сценарий `frontend/tests/e2e/landing/landing-benefits.spec.ts`, который на разных языках сверяет тексты преимуществ и их порядок

**Checkpoint**: Hero-блок и CTA работают, страница `/` даёт мгновенный вход в экосистему

---

## Phase 4: User Story 2 – Локализованный опыт (Priority: P2)

**Goal**: Hero и CTA отображают корректные переводы (en/ru/uk/de), есть переключатель языка
**Independent Test**: Изменить язык через переключатель и убедиться, что тексты обновляются без перезагрузки

### Implementation for User Story 2

- [x] T012 [P] [US2] Дополнить `frontend/src/i18n.ts` переводами `hero_*` и `cta_enter` для ru, uk, de, включая названия языков
- [x] T013 [P] [US2] Реализовать `frontend/src/components/landing/LanguageSwitcher.tsx` с поддержкой клавиатуры и aria-атрибутов
- [x] T014 [US2] Интегрировать `LanguageSwitcher` в `frontend/src/pages/LandingPage.tsx`, прокинув выбранный язык в `HeroSection` и список преимуществ
- [x] T015 [US2] Добавить Playwright тест `frontend/tests/e2e/landing/landing-language.spec.ts`, проверяющий, что смена языка обновляет заголовок и CTA
- [x] T039 [US2] Создать Playwright сценарий `frontend/tests/e2e/landing/landing-autodetect.spec.ts`, который подставляет разные `Accept-Language` значения, проверяет автоопределение языка, подсказку fallback и фиксирует статистику точности для SC-002

**Checkpoint**: Лендинг полностью локализован и остаётся независимым от остальных историй

---

## Phase 5: User Story 3 – Иммерсивный визуальный фон (Priority: P3)

**Goal**: Страница отображает динамический 3D фон (ZenScene) с graceful degradation до poster/градиента
**Independent Test**: На устройстве с WebGL фон анимирован; при `prefers-reduced-motion` или отключённом WebGL появляется статичный poster

### Implementation for User Story 3

- [x] T016 [P] [US3] Создать `frontend/src/components/landing/ZenScene.tsx` и `VisualBackground.tsx`, реализующие новую React Three Fiber сцену (параллакс-слои, камера ±12°) и регулирующие освещение/отражения
- [x] T017 [P] [US3] Добавить CSS для fallback слоёв в `frontend/src/index.css` (градиенты, data-атрибуты) и связанный модуль стилей `frontend/src/components/landing/visual-background.module.css`
- [x] T018 [US3] Интегрировать `VisualBackground` в `frontend/src/pages/LandingPage.tsx`, используя `visualSupport.ts` для выбора WebGL/poster/gradient и подключая `landing-poster.webp`
- [x] T019 [US3] Написать Playwright тест `frontend/tests/e2e/landing/landing-visuals.spec.ts`, который эмулирует `prefers-reduced-motion` и проверяет переключение на fallback
- [x] T036 [US3] Реализовать телеметрию fps/latency в `VisualBackground` (агрегация через `requestAnimationFrame`, таймер fallback) и экспортировать данные в `window.__landingMetrics` + лог `specs/001-modern-home-design/quickstart.md`
- [x] T037 [US3] Создать Playwright сценарий `frontend/tests/e2e/landing/landing-visual-metrics.spec.ts`, который запускает сцену с различными конфигурациями и проверяет, что средний fps ≥45, а fallback активируется ≤500 мс

**Checkpoint**: Иммерсивный фон работает и корректно деградирует, не мешая CTA

---

## Phase 6: Quality Gates & Polish

- [ ] T020 [P] [US1/US2] Реализовать адаптивные responsive-стили в `frontend/src/components/landing/*.tsx` и `index.css`, покрыв брейкпоинты от 320 px до 4K; зафиксировать токены в `tailwind.config.js`
- [ ] T021 [P] [US1] Добавить Playwright тест `frontend/tests/e2e/landing/landing-responsive.spec.ts`, эмулирующий 280 px ширину и проверяющий отсутствие горизонтального скролла и видимость CTA
- [ ] T022 [US1/US2] Настроить a11y/контраст в `HeroSection`, `PrimaryCTA` и `LanguageSwitcher` (aria-labels, focus ring, контраст ≥ WCAG AA) + включить `@axe-core/playwright` smoke
- [ ] T023 [US1/US2] Написать Playwright сценарий `frontend/tests/e2e/landing/landing-accessibility.spec.ts`, который проходит страницу только клавиатурой (≤3 таба до CTA) и запускает axe-проверку
- [ ] T024 [US2] Добавить fallback-ветку языка в `frontend/src/i18n.ts` и тест `frontend/tests/e2e/landing/landing-fallback-language.spec.ts`, подставляющий локаль es и проверяющий подсказку о доступных языках
- [ ] T025 [US3] Реализовать skeleton/gradient fallback в `VisualBackground` и написать тест `frontend/tests/e2e/landing/landing-visuals-fallback.spec.ts`, который замедляет ассеты и проверяет показ skeleton спустя 2 секунды
- [ ] T026 [US1/US3] Настроить Lighthouse скрипт `frontend/scripts/landing-lh.mjs` для проверки TTI ≤2 c на эмуляции Moto G4 3G и добавить шаг CI/README по запуску
- [ ] T027 [US1] Провести UX-тест CTA discoverability (≥5 респондентов), задокументировать результаты и среднее время клика в `specs/001-modern-home-design/quickstart.md`
- [ ] T027 [US1] Провести UX-тест CTA discoverability (≥5 респондентов), задокументировать среднее время клика и долю участников (≥90%), уложившихся <3 секунд, в `specs/001-modern-home-design/quickstart.md`
- [ ] T028 [US1] Зафиксировать методику опроса для SC-004 (понимание ценности) в `specs/001-modern-home-design/quickstart.md` и приложить форму/шаблон интервью
- [ ] T029 [P] Обновить `docs/development/dev-onboarding.md`, описав новый маршрут `/`, работу локализаций, команды тестов (Playwright, Lighthouse, axe) и публикацию UX-результатов
- [ ] T030 Обновить `README.md` (раздел «Веб-интерфейс») и `specs/001-modern-home-design/quickstart.md`, добавив инструкции по responsive/a11y/perf проверкам и прикрепив результаты прогонов
- [ ] T033 [P] [US1] Добавить системный стек шрифтов и `font-display: swap` для компонентов лендинга (Hero, CTA, LanguageSwitcher), документируя поведение при отключённых кастомных гарнитурах
- [ ] T034 [US1] Реализовать тест `frontend/tests/e2e/landing/landing-font-fallback.spec.ts`, который блокирует загрузку кастомных шрифтов и проверяет отсутствие layout shift >2% и корректный CTA
- [ ] T035 Зафиксировать запуск `npm run docs:validate` и `npm run docs:report`, приложив логи/снимки вывода в `.internal/docs/` и ссылку в quickstart.md

---

## Dependencies & Execution Order

### Phase Dependencies

- **Phase 1 → Phase 2**: Setup обязан завершиться до внедрения маршрутов и утилит
- **Phase 2 → User Stories**: Все истории (US1–US3) зависят от маршрута `/`, контента и визуальных утилит
- **Phase 3 → Phase 4/5**: US2 и US3 могут стартовать после Phase 2, но желательно дождаться готового каркаса US1 (CTA/hero) для интеграций
- **Phase 6**: Выполняется после релевантных историй (минимум после US1 для MVP, далее — после US2/US3)

### User Story Dependencies

- **US1**: Зависит только от Foundation; служит базой для остальных
- **US2**: Зависит от US1 (переключатель должен управлять существующим hero) и Foundation
- **US3**: Зависит от Foundation; интеграция в `LandingPage` не требует завершения US2, но должна учитывать структуру US1

### Parallel Opportunities

- В Phase 1 T001–T003 можно делать параллельно (разные файлы)
- В Phase 2 T005 и T006 независимы после добавления маршрута (T004)
- После Phase 2: команды могут параллельно выполнять US2 и US3, пока US1 заканчивает визуальные мелочи
- Все Playwright тесты (T011, T015, T019) можно писать/запускать параллельно после готовности целевых фич

---

## Parallel Execution Examples

### User Story 1

- Одновременно: T007 (i18n EN) и T008 (CTA) — разные файлы
- После них параллельно: T009 (HeroSection) и подготовка теста T011 (используя мок-версию страницы)

### User Story 2

- Одновременно: T012 (переводы) и T013 (LanguageSwitcher)
- После интеграции (T014) можно параллельно готовить e2e тест T015

### User Story 3

- Одновременно: T016 (VisualBackground R3F) и T017 (CSS fallback)
- После интеграции (T018) запускаем тест T019 параллельно с финальной отладкой

---

## Implementation Strategy

### MVP First (User Story 1)

1. Выполнить Phase 1–2 для структуры и маршрута
2. Реализовать US1 (T007–T011) → получить рабочую страницу `/` с CTA
3. Провести демо/сбор фидбэка

### Incremental Delivery

1. После MVP добавить US2 (T012–T015) → локализация
2. Затем US3 (T016–T019) → визуальный фон
3. На каждом шаге обновлять документацию (Phase 6)

### Parallel Team Strategy

- Команда A: US1 (после Foundation)
- Команда B: US2 (может стартовать после готовности HeroSection API)
- Команда C: US3 (работает параллельно, т.к. затрагивает отдельные файлы)
- Общая синхронизация перед Phase 6 (docs + quickstart)
