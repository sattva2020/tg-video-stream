# Tasks: Способы оповещений (по аналогии с Zabbix)

**Input**: Design documents from `/specs/001-alert-notification-methods/`
**Prerequisites**: plan.md (required), spec.md (required), research.md, data-model.md, contracts/
**Tests**: Добавлены задачи на backend/frontend/worker и smoke-путь event→delivery/failover.
**Organization**: Задачи сгруппированы по user story для независимой реализации и тестирования.

## Phase 1: Setup (Shared Infrastructure)

- [x] T001 Обновить зависимости уведомлений в `backend/pyproject.toml` и `backend/requirements.txt` (apprise, aiosmtplib, aiogram, slack_sdk, twilio, httpx)
- [x] T002 Добавить настройки уведомлений (таймауты/ретраи/очередь Redis/Celery) в `backend/src/settings.py` и шаблон окружения `backend/.env.example`
- [x] T003 [P] Объявить очередь `notifications` в `backend/src/worker/app.py` и прописать Celery конфиг (prefetch, acks_late)

---

## Phase 2: Foundational (Blocking Prerequisites)

- [x] T004 Создать Alembic-миграцию `backend/alembic/versions/*_notifications.py` для таблиц Channel/Template/Recipient/Rule/DeliveryLog
- [x] T005 [P] Добавить SQLAlchemy-модели в `backend/src/models/notifications.py` (включая связи и индексы)
- [x] T006 [P] Добавить Pydantic-схемы каналов/шаблонов/получателей/правил/логов в `backend/src/api/schemas/notifications.py`
- [x] T007 Создать базовый сервис/репозиторий уведомлений в `backend/src/services/notifications/base.py` (CRUD + выборки с фильтрами)
- [x] T008 Реализовать Redis-хелперы дедуп/ratelimit/silence windows в `backend/src/services/notifications/controls.py`
- [x] T009 Настроить каркас Celery-воркера и фабрику Apprise/SDK клиентов в `backend/src/services/notifications/worker.py` (ретраи/таймауты по research)

**Checkpoint**: Foundation ready — можно начинать user stories.

---

## Phase 3: User Story 1 - Настройка каналов уведомлений (Priority: P1) 🎯 MVP

**Goal**: Администратор подключает каналы (email/Telegram/webhook и др.), задаёт шаблоны, выполняет тестовую отправку.
**Independent Test**: Для каждого типа канала создать запись, задать шаблон, выполнить `Test` и получить доставку/лог с подставленными переменными.

### Implementation for User Story 1

- [x] T010 [P] [US1] Реализовать CRUD каналов по контракту в `backend/src/api/routes/notifications_channels.py`
- [x] T011 [P] [US1] Реализовать CRUD шаблонов в `backend/src/api/routes/notifications_templates.py`
- [x] T012 [P] [US1] Добавить сервис валидации/тестовой отправки канала (Apprise + SDK) в `backend/src/services/notifications/channels.py`
- [x] T013 [US1] Прокинуть `POST /channels/{id}/test` через воркер и логирование DeliveryLog в `backend/src/api/routes/notifications_channels.py`
- [x] T014 [US1] Добавить UI каналов/шаблонов с тест-отправкой в `frontend/src/pages/notifications/channels.tsx`
- [x] T027 [US1] Добавить флаги enabled/blocked/opt_out в модели/схемы/CRUD каналов и получателей; логировать пропуски в DeliveryLog (reason)
- [x] T028 [US1] Добавить UI-переключатели включения канала и opt-out получателя, отображать статусы/причины пропуска в списках

**Checkpoint**: User Story 1 функциональна и тестируема независимо.

---

## Phase 4: User Story 2 - Маршрутизация и эскалации (Priority: P2)

**Goal**: Правила доставки по severity/тегам/хостам с порядком каналов и резервом.
**Independent Test**: Смоделировать событие; оно уходит по правилу в основной канал, при сбое — в резерв через таймаут и логируется как failover.

### Implementation for User Story 2

- [x] T015 [P] [US2] Реализовать CRUD правил в `backend/src/api/routes/notifications_rules.py` (filters, channels_order, failover_timeout)
- [x] T016 [P] [US2] Реализовать сервис маршрутизации/эскалации в `backend/src/services/notifications/routing.py` (выбор получателей, порядок каналов, ретраи)
- [x] T017 [US2] Добавить endpoint приема событий `/api/notifications/events` и постановку в очередь в `backend/src/api/routes/notifications_events.py`
- [x] T018 [US2] Реализовать `POST /rules/{id}/test` с прогоном маршрутизации в `backend/src/api/routes/notifications_rules.py`
- [x] T019 [US2] Добавить UI правил (фильтры, получатели, порядок каналов, таймаут failover) в `frontend/src/pages/notifications/rules.tsx`

**Checkpoint**: User Stories 1 и 2 работают независимо.

---

## Phase 5: User Story 3 - Снижение шума и обслуживание (Priority: P3)

**Goal**: Подавление шума — окна тишины, rate-limit, дедупликация, агрегирование при шторме.
**Independent Test**: Задать окно тишины и rate-limit, сгенерировать пачку событий; уведомления подавлены или агрегированы, логи содержат статусы suppressed/rate-limited/deduped.

### Implementation for User Story 3

- [x] T020 [P] [US3] Включить дедуп/rate-limit/silence enforcement в пайплайн `backend/src/services/notifications/worker.py` с хелперами `controls.py`
- [x] T021 [US3] Расширить DeliveryLog статусами suppressed/rate-limited/deduped и фильтрами в `backend/src/api/routes/notifications_logs.py`
- [x] T022 [P] [US3] Добавить UI для окон тишины/лимитов и фильтры логов в `frontend/src/pages/notifications/logs.tsx`
- [x] T023 [US3] Добавить агрегирование штормовых событий (batch summary) в `backend/src/services/notifications/worker.py` с настройкой окна по правилу

**Checkpoint**: Все три истории работают независимо.

---

## Phase 6: NFR, Observability & Tests

- [ ] T029 [P] Добавить автотесты backend (модели/схемы/CRUD/API каналов/шаблонов/правил, worker ретраи/failover/dedup/rate-limit/silence)
- [ ] T030 [P] Добавить интеграционный/smoke сценарий event→delivery/failover (Celery+Redis+HTTP/SMTP/Telegram моки) с проверкой логов
- [ ] T031 [P] Добавить e2e/Playwright тесты UI (создание каналов/правил, тест-отправка, фильтрация логов статусами)
- [ ] T031a [P] Проверить UX «быстрая настройка и тест канала ≤5 минут»: сценарий e2e/smoke с таймером (создание канала, шаблона, тест-отправка), отчёт о длительности
- [ ] T032 [P] Добавить метрики latency (event→log) и контроль p95 ≤30s: логировать duration, экспорт Prometheus, проверка в smoke/тестах
- [ ] T033 [P] Добавить метрики шумоподавления (до/после дедуп/лимитов/агрегации) и валидацию снижения ≥70%
- [ ] T034 [US1] Добавить поддержку RU/EN шаблонов (поле языка, хранение/рендер), переключатель в UI и тестовую отправку для двух языков
- [ ] T035 [US3] Гарантировать запись всех попыток (success/fail/failover/suppressed/rate-limited/deduped), экспорт/фильтры и проверки в тестах
- [ ] T036 [P] Зафиксировать дефолты попыток/интервалов/таймаутов/параллелизма в settings и покрыть тестами/документацией

---

## Phase N: Polish & Cross-Cutting Concerns

- [ ] T024 [P] Обновить документацию (spec/plan/quickstart/contracts) в `specs/001-alert-notification-methods/`
- [ ] T025 [P] Добавить smoke-инструкцию/скрипт прогонки события в `tests/notifications/README.md`
- [ ] T026 Провести финальную проверку: линт/формат, alembic heads, docker-compose/env шаблоны

---

## Dependencies & Execution Order

- Setup (Phase 1) → Foundational (Phase 2) → User Stories (P1→P2→P3) → Polish.
- User stories могут идти параллельно после завершения Foundational, но приоритет внедрения: US1 (MVP) → US2 → US3.

## User Story Dependencies

- US1 не зависит от других историй после Foundation.
- US2 зависит от Foundation; может использовать сущности/логирование US1, но должна быть тестируема отдельно.
- US3 зависит от Foundation; работает поверх пайплайна доставки, не ломая US1/US2.

## Parallel Opportunities

- [P] задачи в Setup/Foundational можно выполнять параллельно (T003, T005, T006, T012, T015, T016, T020, T022, T024, T025).
- Внутри US1 параллельно: CRUD (T010, T011) и сервис отправки (T012).
- Внутри US2 параллельно: CRUD правил (T015) и сервис маршрутизации (T016).
- Внутри US3 параллельно: пайплайн подавления (T020) и UI логов/окон (T022).

## Implementation Strategy

- MVP: завершить Phases 1–2, затем US1 (тестовая отправка по каналам) → короткая валидация.
- Инкрементально: добавить US2 (маршрутизация/эскалации) → затем US3 (шумоподавление) с независимыми тестами.
